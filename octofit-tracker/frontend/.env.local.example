# Define this in .env.local when running the frontend from GitHub Codespaces.
# The frontend uses it to call https://$VITE_CODESPACE_NAME-8000.app.github.dev/api/[component]/.
VITE_CODESPACE_NAME=your-codespace-name
